import type { ChartConfig } from "@/components/ui/chart";

export const annualSavingsChartConfig = {
  savings: {
    label: "Ahorro",
    color: "color-mix(in oklch, var(--chart-2) 55%, oklch(0.68 0 0))"
  }
} satisfies ChartConfig;
